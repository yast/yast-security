/**
 * File:
 *   menuentry_security.ycp
 *
 * Module:
 *   Security configuration
 *
 * Summary:
 *   The definition of menu entry
 *
 * Authors:
 *   Michal Svec <msvec@suse.cz>
 *
 * $Id$
 *
 */

/**
 * @return list the menuentry
 */

{

////////////////////////////////////////////////////////////////////////////////////////
// set the textdomain, (generally the name of the module for example "lan" for y2c_lan
// in doubt do a "grep textdomain" in the main module, you are calling 
textdomain "security";
   
return [ "security", $[

     ////////////////////////////////////////////////////////////////////////////////////////
     // name of textdomain, generally the name of the module for example "lan" for y2c_lan //
     `textdomain    : "security",
     
     ////////////////////////////////////////////////////////////////////////////////
     // name of the entry, shown in curses menu and ycc                             //
     `menuentry    : UI(_("Security settings")),
     
     ////////////////////////////////////////////////////////////////////////////////
     //leave blank,  experts only ;-)
     `arguments    : [ ],
     
      ////////////////////////////////////////////////////////////////////////////////
      // group (one of the groups in /usr/lib/YaST2/etc/y2contolcenter.conf         //
     `group        : "Security", 
   
     ////////////////////////////////////////////////////////////////////////////////
     // Icon used in y2contolcenter                                                //
     `icon         : UI(_("security.png")),
     
      ////////////////////////////////////////////////////////////////////////////////
      // explanation of yast2 menueentry in ncurses menu                            //                 
     `widget       : `RichText(UI(_("Configure all local security settings.
<p>You need to be logged in as <i>root</i>
in order to do this.</p>"))),
     
      ////////////////////////////////////////////////////////////////////////////////
      // explanation in YaST2 Control Center                                         //
      `helptext     : UI(_("Configure all local security settings")),
   
      ////////////////////////////////////////////////////////////////////////////////
      // leave nil, experts only ;-)
      `codefragment : nil 
   
   ]
];

/* EOF */
}
