Security Module TODO file
-------------------------
$Id$

DOC:
../src/ui.ycp

Urgent:
- grep FIXME *.ycp
- ROOT_LOGIN_REMOTE "yes"|"No" hack!
- helps: useradd -- adduser
- STRINGS -> HELPS();
- settings.ycp -> items.ycp?
- RcConfig_settings -> Settings_RcConfig
- rename includes
- reorder functions (see impl.txt)
- permissions: local (replace or add?) -- maybe "Add local perms" [X]

IO:
- updateValue checking val and bounds
- map2widget checking the Value
- error handling (no .etc.rc.system, ...)

UI:
- simplify the SubDialogs updateValues(ret,list), SuSEDialog(list IDs), ...
- BottomButtons(list buttons)
- enhance layout of SuSEDialog, ...
- use Notebook widget (not ready yet)

Future:
- add other settings (login_defs.txt, and see bottom...)
- easy/advanced (Server|Workstation|NetStation|...)

New features:
- limits [limits.conf, ulimit]
- services [cron, at, lpd]
- devices [floppy, cdrom, sound, scanner, printer?, ppp?, ...]
- software [dosemu, sudo, emulators, ppp?, ...]

Files for these:
- securetty, pam.d/*, security/access.conf, ssh/sshd_config
- cron.allow, cron.deny, ... 
- security/limits.conf
- lilo.conf
- unsure?: inittab, kdmrc

EOF
